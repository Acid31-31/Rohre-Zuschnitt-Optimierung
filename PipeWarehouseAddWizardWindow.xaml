<Window x:Class="RohreZuschnittOptimierung.PipeWarehouseAddWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Material anlegen"
        Height="620"
        Width="720"
        MinHeight="500"
        MinWidth="640"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock x:Name="TitleTextBlock"
               Grid.Row="0"
               FontSize="20"
               FontWeight="SemiBold"
               Foreground="{DynamicResource SectionHeaderBrush}"/>

    <TextBlock x:Name="StepTextBlock"
               Grid.Row="1"
               Margin="0,10,0,16"
               TextWrapping="Wrap"
               Foreground="{DynamicResource TextSecondaryBrush}"/>

    <Border Grid.Row="2"
            Padding="16"
            BorderBrush="{DynamicResource PanelBorderBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Background="{DynamicResource PanelBackgroundBrush}">
      <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
          <WrapPanel x:Name="ChoiceButtonsPanel" Margin="0,0,0,12"/>

          <StackPanel x:Name="InputPanel" Visibility="Collapsed">
            <TextBlock x:Name="InputLabelTextBlock"
                       FontWeight="SemiBold"
                       Margin="0,0,0,8"/>
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
              </Grid.ColumnDefinitions>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
              </Grid.RowDefinitions>
              <TextBlock x:Name="LengthLabel"
                         Grid.Row="0" Grid.Column="0"
                         Text="Länge (mm)"
                         Margin="0,0,0,4"
                         Visibility="Collapsed"/>
              <TextBox x:Name="LengthTextBox"
                       Grid.Row="1" Grid.Column="0"
                       Visibility="Collapsed"/>
              <TextBlock Grid.Row="0" Grid.Column="2"
                         Text="Stückzahl"
                         Margin="0,0,0,4"/>
              <TextBox x:Name="QuantityTextBox"
                       Grid.Row="1" Grid.Column="2"
                       Text="1"/>
            </Grid>
          </StackPanel>

          <TextBlock x:Name="SummaryTextBlock"
                     Margin="0,12,0,0"
                     TextWrapping="Wrap"
                     Foreground="{DynamicResource TextMutedBrush}"/>
        </StackPanel>
      </ScrollViewer>
    </Border>

    <Grid Grid.Row="3" Margin="0,18,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="12"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <Button Content="Zurück"
              Style="{DynamicResource SecondaryButtonStyle}"
              Click="Back_Click"
              Padding="14,8"
              x:Name="BackButton"
              IsEnabled="False"/>
      <Button Grid.Column="2"
              x:Name="NextButton"
              Content="Weiter"
              Click="Next_Click"
              Padding="14,8"
              IsEnabled="False"/>
      <Button Grid.Column="4"
              Style="{DynamicResource SecondaryButtonStyle}"
              Content="Abbrechen"
              Click="Cancel_Click"
              Padding="14,8"
              IsCancel="True"/>
    </Grid>
  </Grid>
</Window>

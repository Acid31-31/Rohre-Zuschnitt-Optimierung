<Window x:Class="RohreZuschnittOptimierung.PdfSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Einstellungen"
        Height="460"
        Width="560"
        MinHeight="400"
        MinWidth="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0"
               Text="Einstellungen"
               FontSize="20"
               FontWeight="SemiBold"
               Foreground="{DynamicResource SectionHeaderBrush}"/>

    <TextBlock Grid.Row="1"
               Margin="0,10,0,16"
               TextWrapping="Wrap"
               Foreground="{DynamicResource TextSecondaryBrush}"
               Text="Bereich wählen. Jede Kategorie hat ein eigenes Fenster."/>

    <Border Grid.Row="2"
            Padding="16"
            BorderBrush="{DynamicResource PanelBorderBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Background="{DynamicResource PanelBackgroundBrush}">
      <UniformGrid Columns="2" Rows="2">
        <Button Margin="0,0,8,8"
                Padding="16,18"
                HorizontalContentAlignment="Left"
                Click="OpenGeneral_Click"
                Style="{DynamicResource SecondaryButtonStyle}">
          <StackPanel>
            <TextBlock Text="Optimierung"
                       FontSize="15"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource SectionHeaderBrush}"/>
            <TextBlock Margin="0,6,0,0"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       Text="Originalstange und Schnittbreite"/>
          </StackPanel>
        </Button>
        <Button Margin="8,0,0,8"
                Padding="16,18"
                HorizontalContentAlignment="Left"
                Click="OpenNetwork_Click"
                Style="{DynamicResource SecondaryButtonStyle}">
          <StackPanel>
            <TextBlock Text="Netzwerk"
                       FontSize="15"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource SectionHeaderBrush}"/>
            <TextBlock Margin="0,6,0,0"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       Text="Lager-Zentrale für mehrere PCs"/>
          </StackPanel>
        </Button>
        <Button Margin="0,8,8,0"
                Padding="16,18"
                HorizontalContentAlignment="Left"
                Click="OpenPdf_Click"
                Style="{DynamicResource SecondaryButtonStyle}">
          <StackPanel>
            <TextBlock Text="PDF-Zuschnittplan"
                       FontSize="15"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource SectionHeaderBrush}"/>
            <TextBlock Margin="0,6,0,0"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       Text="Inhalte und Schnittfolge im PDF"/>
          </StackPanel>
        </Button>
        <Button Margin="8,8,0,0"
                Padding="16,18"
                HorizontalContentAlignment="Left"
                Click="OpenVisionAi_Click"
                Style="{DynamicResource SecondaryButtonStyle}">
          <StackPanel>
            <TextBlock Text="Vision-KI"
                       FontSize="15"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource SectionHeaderBrush}"/>
            <TextBlock Margin="0,6,0,0"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       Text="Lokale KI für Länge und Gehrung"/>
          </StackPanel>
        </Button>
      </UniformGrid>
    </Border>

    <Button Grid.Row="3"
            Margin="0,18,0,0"
            HorizontalAlignment="Right"
            Style="{DynamicResource SecondaryButtonStyle}"
            Content="Schließen"
            Click="Close_Click"
            Padding="14,8"
            IsCancel="True"/>
  </Grid>
</Window>

<Window x:Class="RohreZuschnittOptimierung.UpdateApplyProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rohre Zuschnitt Optimierung wird aktualisiert"
        Height="240" Width="500"
        MinHeight="220" MinWidth="440"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        Topmost="True">
  <Border Background="{DynamicResource AppBackgroundBrush}"
          BorderBrush="{DynamicResource PanelBorderBrush}"
          BorderThickness="1"
          Padding="24">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <TextBlock Grid.Row="0"
                 Text="Update wird installiert…"
                 FontSize="16"
                 FontWeight="SemiBold"
                 Foreground="{DynamicResource TextPrimaryBrush}"
                 Margin="0,0,0,16"/>

      <ProgressBar x:Name="InstallProgressBar"
                   Grid.Row="1"
                   Height="12"
                   Minimum="0"
                   Maximum="100"
                   Value="0"
                   Foreground="{DynamicResource AccentBrush}"
                   Background="{DynamicResource PanelBorderBrush}"/>

      <Grid Grid.Row="2" Margin="0,12,0,0">
        <TextBlock x:Name="StatusTextBlock"
                   FontSize="12"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   Text="Vorbereitung…"
                   TextWrapping="Wrap"
                   VerticalAlignment="Center"/>
        <TextBlock x:Name="PercentTextBlock"
                   HorizontalAlignment="Right"
                   FontSize="12"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource AccentBrush}"
                   VerticalAlignment="Center"
                   Text="0 %"/>
      </Grid>

      <TextBlock x:Name="RemainingTimeTextBlock"
                 Grid.Row="3"
                 Margin="0,8,0,0"
                 FontSize="12"
                 FontWeight="SemiBold"
                 Foreground="{DynamicResource AccentBrush}"
                 Text="Restlaufzeit wird berechnet…"/>
    </Grid>
  </Border>
</Window>
